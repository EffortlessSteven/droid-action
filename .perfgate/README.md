# perfgate setup

Generated by `perfgate init`.

## Files

- `perfgate.toml`: benchmark commands, budgets, baseline location, and artifact path.
- `.github/workflows/perfgate.yml`: CI performance gate workflow.

- `baselines/`: checked-in local baselines. Keep `.gitkeep` until the first baseline is promoted.
- `artifacts/perfgate/`: local and CI output receipts, reports, and PR comment markdown.

## Next

1. Review the `droid-node-version` command in `perfgate.toml`.
2. Run `perfgate check --config perfgate.toml --all --require-baseline`.
3. Inspect `artifacts/perfgate/droid-node-version/comment.md` and `repair_context.json`.
4. Keep server ledger mode out of this canary; local receipts are the correctness contract.
